{
  "name": "@demo-locker/player",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "vite build && tsc -p tsconfig.build.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "happy-dom": "^20.10.6",
    "typescript": "^5.7.0",
    "vite": "^8.0.1",
    "vitest": "^4.1.10"
  }
}
